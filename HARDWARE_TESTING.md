# 同伴测试入口：软件复跑、EDA 与实物验收

本次改动可由其他同伴直接复跑软件测试；有 EDA 或设备样机的同伴，可以继续补齐下列集成与实物记录。请选择自己能完成的任务，并用文末模板回传结果。**本仓库提供 Skill 与检查工具；这里没有声称某一台录音设备已经完成实物验收。**

## 当前验证范围

2026-09-26 的 `v2.0.1-dev` 本地结果：475 项 Python 测试，零失败或错误，其中 1 项 Windows junction 专用测试在 macOS 跳过；81 项 Node 测试通过，隔离的模拟桥接测试通过。详情见 [VALIDATION.md](VALIDATION.md)。

| 范围 | 当前证据 | 同伴可以补充什么 |
|---|---|---|
| 检查与进度逻辑 | 必需检查错误使用 `N_A`、G8 进度缺项已有回归用例 | 在不同电脑复跑，记录失败输入和输出 |
| 整机成本计算 | 已检查报价口径、PCBA 包含元件时不重复计价、未知价格及首件/复做金额计算 | 用明确来源的实际报价复核；未知价格仍保持未知 |
| EDA 桥接与原生文件 | 本次为模拟客户端回归；未新增当前版本的真实 EDA 操作记录 | 真实客户端连接、原生对象读回、保存重开和相关检查 |
| 录音、传输、转写和摘要 | 本次未做设备或完整应用实测 | 按下文保留同一录音的完整证据链 |
| 充电、供电、续航、装配 | 本次未做实物测量 | 按具体板卡、电池和器件手册填写判据后测量 |

软件回归通过只覆盖被执行的软件用例。未做的测试填写 `NOT_RUN`；缺少样机、固件、接收程序、型号或判据时填写 `BLOCKED` 并说明缺什么。完成测试后才填写 `PASS` 或 `FAIL`。不适用的项目注明设计依据，不把它计作通过。

## A. 我可以帮忙复跑软件

在当前待测分支/提交的仓库根目录执行。先记录 `git rev-parse HEAD`、操作系统、Python 和 Node 版本。需要 Python 3.10+、Node.js 18+；完整套件中有关栅格与铜间距的检查还需要 NumPy，缺少依赖时在结果中记录，不把未运行项目计作通过。

macOS / Linux：

```sh
git rev-parse HEAD
python3 --version
node --version
mkdir -p ../pcb-regression-work
python3 scripts/run_regression.py --workdir ../pcb-regression-work --allow-skips > ../pcb-regression-work/regression.log 2>&1
```

这里使用 `--allow-skips` 是因为 Windows junction 用例不能在 macOS / Linux 执行。查看日志最后的 JSON 汇总以及 Node 测试结果，回传全部 `skips` 的名称和原因；此命令的退出成功不代表零跳过发布验证。若出现额外跳过，请单独说明。

Windows PowerShell：

```powershell
git rev-parse HEAD
py -3 --version
node --version
$testWork = Join-Path $env:TEMP "pcb-skill-regression"
New-Item -ItemType Directory -Force -Path $testWork | Out-Null
py -3 scripts/run_regression.py --workdir $testWork *> (Join-Path $testWork "regression.log")
$LASTEXITCODE
```

Windows 默认不加 `--allow-skips`，用于补跑 junction 用例。若系统权限或环境导致用例跳过，保留原始原因；不要仅通过加选项把它报告为已覆盖。回传日志、提交号、失败/错误/跳过数量及最后 JSON；如果只用了 `--python-only`，注明 Node 和桥接未运行。

## B. 我有 EDA，可以补集成测试

按 [启动指南](START_HERE.md) 与 [EDA 执行说明](references/06-easyeda-execution.md)配置当前客户端，在自己可编辑的测试项目副本中复现。开始前写下要执行的操作、期望变化和需要保留的对象。

1. 记录客户端、Gateway、Skill 提交号和工程版本；确认连接到所记录的测试工程。
2. 读取当前原理图/PCB 的原生对象，核对关键器件完整 MPN、符号引脚、BOM、封装焊盘与网络，保存读回记录。
3. 选择一个项目需要的最小操作，比较操作前后原生数据及画面；随后保存、关闭、重开，核对修改仍在且相关网络/属性符合预期。
4. 执行与该操作相关的 ERC/DRC 或仓库检查，回传原生工程/导出文件、结果及未解决项。若测试回滚，再单独记录回滚前后对比。

只做了连接、读取或一个对象的修改，就按该范围报告；不要把它写成整板布线、可制造性或实板通过。

## C. 我有设备样机，可以补实物测试

先复制 [整机功能与硬件验收模板](assets/zh/PRODUCT_ACCEPTANCE.template.md)到具体项目，按 [完整型号与整机验收说明](references/38-part-identity-and-device-acceptance.md)补齐信息。项目提供者应给出可用的接线/刷机说明、固件、电脑接收程序和测试音频；没有这些材料时，先记录 `BLOCKED` 和缺少的材料。

### 测试前填写条件和判据

| 项目 | 开始前必须确定 |
|---|---|
| 样机身份 | 板卡/PCB 版本、实板 ID，关键芯片/模块/麦克风的厂家完整 MPN（保留后缀），电池确切型号和规格来源 |
| 软件身份 | 固件提交/哈希，电脑 OS，接收程序版本，转写/摘要服务及模型版本 |
| 按键和指示 | 按下/松开/取消动作，录音/传输/完成/失败的指示，连续操作和异常操作的预期 |
| 音频 | 采样率、声道、位深或编码、最长录音时长、允许的质量/采样丢失范围 |
| 传输 | BLE/USB/其他实际协议，测试距离/场景、最长等待时间、重复次数、超时和重试处理 |
| 内容结果 | 固定语音样本和正确文本；数字、否定词、中英词的核对方式；一句摘要 + 三个要点的内容判据 |
| 供电 | 实际电池与充电芯片手册章节、工作模式、测试点、仪器、环境与各状态允许范围 |
| 续航 | 使用脚本、各状态时间占比、起始电量、结束条件、目标时间和温度 |

电压、电流、温度和终止条件必须来自**所选完整型号、电池和当前电路**的要求；本入口不为未知板卡指定通用阈值。测试中途改变判据，应注明原因并重新标记受影响结果。

### 录音到 AI 结果

每次录音使用一个稳定的 `recording ID`，串联下面的文件和日志：

```text
物理按键 → 设备麦克风 → 设备缓存/文件 → 电脑保存原始音频
         → 转写文本 → 一句摘要 + 三个要点
```

| 任务 | 做什么 | 回传什么、如何判断 |
|---|---|---|
| 电脑端基线 | 用已知音频经过接收/转写/摘要路径 | 输入文件、转写和摘要；按事前内容判据核对。设备采集仍记 `NOT_RUN` |
| 按键与真实采集 | 执行开始、结束、取消及重复操作，用设备实际麦克风录音 | 按键/指示时间，电脑端原始音频，预期与实际时长/采样数；独立播放核查截断、静音和削波 |
| 最长与连续录音 | 按声明的最长时长和重复次数运行 | 实际大小、可用存储、采样计数、传输耗时、连续运行日志；逐项对照事前判据 |
| 断连与重试 | 在传输时断连、关闭接收程序，恢复后重试；测试超时与重启 | 原始事件日志与最终记录，证明状态可见，且没有静默丢失或重复最终记录；保留同一 recording ID |
| 完整 AI 结果 | 对同一份设备原始音频转写并生成一句摘要和三个要点 | 逐项对照原音，检查数字、否定和中英词；摘要每个事实均可追溯至转写和原音 |

最长录音先做存储核算：`采样率 × 声道数 × 位深 / 8 × 秒数`（未压缩 PCM；编码音频按实际格式计算），再用实测吞吐与耗时验证。蓝牙配对或 HID 输入成功不能替代音频传输结果。

### 充电、供电与续航

根据当前设计选择适用状态，按已填写的范围测量并保存原始记录：

- 仅电池：待机、录音、传输、睡眠和唤醒；记录电池端及系统电源轨、电流、复位和功能表现。
- USB + 电池：待机与最大实际负载、低电量与充满后的状态；分别测量输入电流和电池电流，核对充电/补充供电状态及温度趋势。
- 仅在设计支持时测 USB + 无电池、输入限流、温度检测或故障恢复；不支持的状态注明依据。
- 续航估算：在同一电池侧测量边界取得各状态电流，时间占比合计为 1。记录 `I_avg = Σ(I_state × 占比)` 和 `预计小时 = 可用容量 / I_avg`；不同电压轨需按功率与实际转换效率换算。
- 续航实测：从定义好的起始电量运行同一使用脚本，直至设计的结束条件；记录实际时长、电池/板号/固件、温度和事件。估算与实测分栏填写。

LED 或充电状态脚只能作为观察项，充电结论须附电池电流等测量。改板、换料、换电池或软件后，列出受影响项再复验。

## 复制到 GitHub Issue 的结果模板

标题建议：`[测试回传][软件/EDA/实物] 提交短号 — 测试项目`

```markdown
### 本次范围与状态
- 类型：软件复跑 / EDA / 实物
- 总体状态：NOT_RUN / BLOCKED / PASS / FAIL
- 实际测试日期：
- Skill 分支与完整 commit：
- 测试项目/需求来源：
- 本次实际执行范围：
- 未执行或不适用项目与原因：

### 环境与样机
- OS、Python、Node（软件复跑）：
- EDA / Gateway 版本及工程版本（EDA）：
- PCB / 板卡版本、实板 ID（实物）：
- 关键器件厂家完整 MPN、封装及 BOM 位号：
- 电池确切型号、规格来源与本次状态：
- 固件版本/哈希，接收程序版本：
- 转写/摘要服务与模型版本：
- 仪器、测点、供电来源和环境：

### 事前条件与结果
| 检查 ID / 动作 | 条件、次数、事前判据及来源 | 实际值 / 行为 | 状态 | 证据文件或链接 |
|---|---|---|---|---|
| 待填写 | 待填写 | 待测 | NOT_RUN | 待填写 |

### 复现与证据
- 软件命令、退出码、最后 JSON、Node 汇总及全部 skip 原因：
- 最小复现步骤：
- 预期结果 / 实际结果：
- recording ID，原始音频 / 正确文本 / 转写 / 摘要：
- 原生工程、日志、采样数/大小/传输耗时及测量表：
- 失败或中断发生在哪一步，重试后是否丢失或重复：
- 已尝试修改及修改后的复验结果：
- 下一步缺少的材料或需要协助的具体任务：
```

回传可下载的原始文件和对应版本；仅有截图或口头“成功”时，注明尚不能复核的部分。项目正式记录继续使用原有 `requirements.json`、`requirement-checks.json`、`CHECKS.csv` 与 `HANDOFF.md`，本入口用于组织同伴测试与收集反馈。
